{
  "entities": {
    "Order": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Order",
      "type": "object",
      "description": "Represents an order in the system.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Order entity."
        },
        "estado": {
          "type": "string",
          "description": "The status of the order (New, On Hand/Working, Urgent, Pending, Packaging, Done)."
        },
        "subEstado": {
          "type": "string",
          "description": "The sub-status of the order (Pendiente Arte, Pendiente, Corte, Pendiente Material, Check List)."
        },
        "name": {
          "type": "string",
          "description": "The name of the customer."
        },
        "email": {
          "type": "string",
          "description": "The email address of the customer.",
          "format": "email"
        },
        "celular": {
          "type": "string",
          "description": "The phone number of the customer."
        },
        "description": {
          "type": "string",
          "description": "A description of the order (max 300 chars)."
        },
        "fechaIngreso": {
          "type": "string",
          "description": "The date and time the order was entered into the system.",
          "format": "date-time"
        },
        "comentarios": {
          "type": "string",
          "description": "Additional comments about the order."
        },
        "entrega": {
          "type": "string",
          "description": "The date the order is to be delivered.",
          "format": "date"
        },
        "entregaLimite": {
          "type": "string",
          "description": "The latest date the order can be delivered.",
          "format": "date"
        },
        "servicioEntrega": {
          "type": "string",
          "description": "The delivery service used (Uno Express, Delivery Rolando, Delivery Daniel, Delivery Otros, Uber, InDriver, Retiro taller)."
        },
        "direccionEnvio": {
          "type": "string",
          "description": "The shipping address for the order."
        },
        "privacidad": {
          "type": "string",
          "description": "The privacy setting for the order (Por preguntar, Limitado Fecha, no respondió, ilimitado, Limitado Otros)."
        },
        "customTag1": {
          "type": "string",
          "description": "Custom tag 1 for the order."
        },
        "customTag2": {
          "type": "string",
          "description": "Custom tag 2 for the order."
        },
        "customTag3": {
          "type": "string",
          "description": "Custom tag 3 for the order."
        },
        "customTag4": {
          "type": "string",
          "description": "Custom tag 4 for the order."
        },
        "productos": {
          "type": "array",
          "description": "An array of product objects associated with the order.",
          "items": {
            "type": "string"
          }
        },
        "orderTotal": {
          "type": "number",
          "description": "The total value of the order."
        }
      },
      "required": [
        "id",
        "estado",
        "subEstado",
        "name",
        "email",
        "celular",
        "description",
        "fechaIngreso",
        "comentarios",
        "entrega",
        "entregaLimite",
        "servicioEntrega",
        "direccionEnvio",
        "privacidad",
        "customTag1",
        "customTag2",
        "customTag3",
        "customTag4",
        "productos",
        "orderTotal"
      ]
    },
    "Product": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Product",
      "type": "object",
      "description": "Represents a product within an order.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Product entity."
        },
        "orderId": {
          "type": "string",
          "description": "Reference to Order. (Relationship: Order 1:N Product)"
        },
        "name": {
          "type": "string",
          "description": "The name of the product."
        },
        "quantity": {
          "type": "number",
          "description": "The quantity of the product ordered."
        },
        "price": {
          "type": "number",
          "description": "The price of the product."
        },
        "materialsReady": {
          "type": "boolean",
          "description": "Indicates whether the materials for the product are ready."
        }
      },
      "required": [
        "id",
        "orderId",
        "name",
        "quantity",
        "price",
        "materialsReady"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/orders/{orderId}",
        "definition": {
          "entityName": "Order",
          "schema": {
            "$ref": "#/backend/entities/Order"
          },
          "description": "Represents an order in the system. Includes denormalized fields if ownership or role-based restrictions are implemented in the security rules.",
          "params": [
            {
              "name": "orderId",
              "description": "The unique identifier for the order."
            }
          ]
        }
      },
      {
        "path": "/orders/{orderId}/products/{productId}",
        "definition": {
          "entityName": "Product",
          "schema": {
            "$ref": "#/backend/entities/Product"
          },
          "description": "Represents a product associated with an order.",
          "params": [
            {
              "name": "orderId",
              "description": "The unique identifier for the order."
            },
            {
              "name": "productId",
              "description": "The unique identifier for the product."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to manage orders and products, focusing on ease of querying, data integrity, and scalability. The primary collection is `/orders`, containing documents representing individual orders. A subcollection `/orders/{orderId}/products` stores products associated with each order. This structure facilitates easy retrieval of all products for a specific order. The design ensures Authorization Independence by not requiring any `get()` calls in the rules, as no authorization data needs to be fetched from parent documents. The structure supports the required QAPs (Rules are not Filters) by ensuring all documents in a collection share the same security requirements.\n\nDenormalization is not strictly required in this case because the security rules primarily rely on user roles which, in this example, are not defined. In a production environment, if access to orders needed to be restricted based on user roles or ownership, those roles or ownership details would be denormalized into each order document."
  }
}